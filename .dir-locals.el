((emacs-lisp-mode
  .
  ((elisp-autofmt-load-packages-local . ("cl-generic" "cl-macs" "cl-seq"))
   ;; This defaults to t, but set it explicitly in case it's been modified globally.
   (sentence-end-double-space . t))))
